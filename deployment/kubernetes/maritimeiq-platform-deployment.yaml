apiVersion: apps/v1
kind: Deployment
metadata:
  name: maritimeiq-platform-api
  namespace: maritimeiq-platform
  labels:
    app: maritimeiq-platform-api
    version: v1
    component: api
    company: maritimeiq-platform
spec:
  replicas: 3
  selector:
    matchLabels:
      app: maritimeiq-platform-api
  template:
    metadata:
      labels:
        app: maritimeiq-platform-api
        version: v1
    spec:
      containers:
      - name: maritimeiq-platform-api
        image: maritimeiqacr.azurecr.io/maritimeiq-platform-api:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: maritimeiq-secrets
              key: sqlconnection
        - name: AzureStorage__ConnectionString
          valueFrom:
            secretKeyRef:
              name: maritimeiq-secrets
              key: storageconnection
        - name: ServiceBus__ConnectionString
          valueFrom:
            secretKeyRef:
              name: maritimeiq-secrets
              key: servicebusconnection
        - name: ApplicationInsights__ConnectionString
          valueFrom:
            secretKeyRef:
              name: maritimeiq-secrets
              key: appinsightsconnection
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: maritimeiq-platform-api-service
  namespace: maritimeiq-platform
spec:
  selector:
    app: maritimeiq-platform-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: LoadBalancer

---
apiVersion: v1
kind: Secret
metadata:
  name: maritimeiq-secrets
  namespace: maritimeiq-platform
type: Opaque
data:
  sqlconnection: # Base64 encoded connection string
  storageconnection: # Base64 encoded connection string  
  servicebusconnection: # Base64 encoded connection string
  appinsightsconnection: # Base64 encoded connection string